<div class="mb-3 row">
    <label for="{{ $tittle }}" class="col-sm-2 col-form-label">{{ $tittle }}</label>
    <div class="col-sm-10">
        <input type="{{ $tipe }}" class="form-control" name="{{ $send }}" value="{{ $value }}">
    </div>
</div>
